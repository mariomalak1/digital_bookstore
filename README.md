# digital_bookstore

### basic two endpoints 
#### POST /api/inventory/upload -> CSV file sent with name 'file'
#### GET  /api/store/:store_id/download-report 

### to run the project

```bash
docker-compose up --build -d
```

```bash
## to run application without docker 
npm run
```
